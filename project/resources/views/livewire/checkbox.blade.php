<form>
    <div class="page_left flex items-baseline">
        <input type="checkbox" id="{{$checkbox_name}}" name="{{$checkbox_name}}"  />
        <div class="ml-4 mt-2 check_text">
            <label for="{{$checkbox_name}}" class="color text-poppins lg:text-lg sm:text-sm">
            {{$checkbox_text}}
            </label>
        </div>
    </div>
</form>